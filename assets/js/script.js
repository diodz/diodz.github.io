// Import the Navbar component here if using a bundler
// import Navbar from './Navbar';

// If not using a bundler, ensure the Navbar component is defined before this script runs

ReactDOM.render(
    <React.StrictMode>
      <Navbar />
    </React.StrictMode>,
    document.getElementById('navbar')
  );
  
  // You can add more JavaScript functionality here as needed